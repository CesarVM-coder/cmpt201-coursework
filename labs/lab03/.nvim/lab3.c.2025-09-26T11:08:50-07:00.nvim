#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>

int main() {

  char *line = NULL;
  size_t len = 0;

  printf("Enter input: ");
  ssize_t nread = getline(&line, &len, stdin);
  if (nread == -1) {
    perror("getline() failed");
    exit(1);
  }

  char *arr[5];

  return 0;
}
